spring.application.name=busplot

# Listen on all interfaces so phone/emulator can reach backend
server.address=0.0.0.0

# Path to bus stops JSON file. Override with an absolute path for cloud deployments.
busplot.data.bus-stops-path=classpath:data/bus_stops.json
